# Comps-Tab Recon — Phase 4a Step 1 (cell map + source-tier mechanism)

**Generated:** 2026-06-29 · **Scope:** read-only recon. No producer, no binding, no contract change.

## The three comp tabs (all currently BLANK)
| Tab | Dimension | Sub-table kind | Producer bindings |
|---|---|---|---|
| Sales Comps | B1:U56 | sale / valuation comps | **0** (no producer) |
| CMBS Comps | B1:U57 | loan-level comps | **0** (no producer) |
| Lease Comps | B1:AA56 | lease comps | **0** (no producer) |

★ **No comps producer exists in the render pipeline** — `grep "slot: 'Comparables_*'"` in render-schema = 0. The tabs render only the template's static CoStar-style scaffolding (placeholder "Comp 1/2/3" + "MAP" regions); nothing is wired.

## Layout — CoStar-export block grid (NOT a flat table)
The cell-walk (CMBS Comps rows 11-32) shows a **multi-row block per comp** with embedded map regions, not one-row-per-comp:
- **r6** = attribute header row (horizontal).
- **r11-12** = Subject block (subject values across cols B-Q).
- **r13-~28** = Comp 1 block — attribute cells (r14 full C-Q), then a **MAP** image region (r22-28).
- **r29+** = next comp block (labels show "Comp 3" col B + "Comp 4" col C same row → comps appear arranged **2-per-band in side-by-side column groups**, each a block with a map).
- Grid runs to row 56-57 → **~6 comp slots (≈2-per-band × 3 bands)** plausibly fit a top-6, BUT:

★★ **FLAG for 4b:** the exact per-comp-block cell coordinates (which cell in each block holds Loan Name vs Balance vs Occup, and the 2-per-band column offsets) require a **precise per-block cell-walk before binding** — this is a CoStar export, not a clean table. Capacity for top-6 is *likely* but must be confirmed against the actual block count; if only ~3 blocks render cleanly, the deliverable may need to be **top-3** or the layout extended.

## CMBS Comps — attribute columns (row 6) → LOAN-level (our primary home)
| Col | Header |
|---|---|
| C | Loan Name |
| D-F | Address |
| G | City, State |
| H | Distance: |
| I | Loan Status: |
| K | Year Built: |
| L | Year Renov: |
| M | Occup: |
| N | Deal: |
| O | Original Balance: |
| P | Current Debt Yield: |
| Q | OPB / Measure: |

## Sales Comps — attribute columns (row 6) → VALUATION
| Col | Header |
|---|---|
| C | Building Name |
| D-F | Address |
| G | City, State |
| H | Distance: |
| I | Direction: |
| K | Year Built: |
| L | Year Renov: |
| M | Occup at Sale: |
| N | Sale Date |
| O | Sale Price |
| P | Cap Rate: |
| Q | Price / Measure |

★ **CMBS tab = loan metrics** (Balance, Debt Yield, $/SF) → fed by our loan-level CompRecord. **Sales tab = valuation** (Sale Price, Cap Rate, $/SF) → fed by our `value`/`capRate`, BUT EX-102 carries **appraised securitization value, not a sale price** — a basis caveat (see field map decision B).

## Lease Comps — decision-C deliberate blank
Lease grid (rent/term/TI columns). **EX-102 has no lease comps** → labeled blank: *"Lease comps require appraisal/broker source — not in SEC filings (EX-102 carries loan/sale data only)."*

## Source-tier / cell treatment mechanism (what actually exists)
- **`MISSING_DATA_FILL_ARGB = 'FFFFC7CE'`** (pink) — the missing/awaiting-data cell fill.
- **`HITL_FILL_ARGB = 'FFD9D9D9'`** (gray) — human-in-the-loop fill.
- **Provenance pattern** (from the Environmental binding precedent): a **`Source: …` note/comment string** carried on the cell + binding comment — the honest-provenance label, NOT a color.

★★ **No "Tier A blue / B yellow / C red" 3-tier source-COLOR system found** in the API services (the brief assumed one). The two fills above are **data-STATE** (missing / HITL), not source-tier. ★ **The consistent home for the SEC source-tier is the provenance-note pattern** (a `SEC EX-102 · <deal> · asset#<n> · filed <date>` label, mirroring the environmental `Source:` string), with `MISSING_DATA_FILL` for genuinely blank cells. If a tier-color legend exists elsewhere (web app / a legend tab), point me to it — otherwise the note pattern is the slot-in.

## Next: field map (Step 2) → 2026-06-29-comps-fieldmap.md
